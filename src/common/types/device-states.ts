// The reason I used this this way is because of my lack of knowledge regarding TS
// I tried to use enum but the time I was spending learning that would affect the delivery of 
// the project within the specified date, so I chose to do this way.

const DEVICE_STATES = ['available', 'in-use','inactive'];

export default DEVICE_STATES;